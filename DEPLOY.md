# 部署到 GitHub Pages

本项目已配置好一键部署到 GitHub Pages 的功能。

## 部署步骤

### 1. 安装依赖

```bash
npm install
```

### 2. 部署项目

```bash
npm run deploy
```

这个命令会：
1. 自动构建项目（`npm run build`）
2. 使用 `gh-pages` 工具将 `dist` 目录推送到 `gh-pages` 分支

### 3. 配置 GitHub Pages

在你的 GitHub 仓库设置中：
1. 进入 **Settings** → **Pages**
2. 在 **Source** 中选择 `gh-pages` 分支
3. 点击 **Save**

几分钟后，你的应用就可以通过以下地址访问：
```
https://Aning-Q.github.io/read-something/
```

## 功能说明

### 跨设备同步

应用把当前设备上的全部数据打包成一份存档，在浏览器里加密后上传到云端；
在新设备上输入同一个同步密码即可恢复。同步层是可插拔的，目前支持两种后端。

| | Supabase | GitHub Gist |
|---|---|---|
| 适合场景 | 完整存档、多设备长期使用 | 轻量同步、不想自建服务 |
| 单次容量 | 受套餐配额限制，通常足够 | 约 1MB，超出会被拒绝 |
| 登录方式 | 邮箱 + 密码 | GitHub Token |
| 历史版本 | 支持，默认保留 5 份 | 只保留最新一份 |

无论选择哪种方式，存档都会先在本机用同步密码加密，云端只能看到密文。

### 方式一：Supabase（推荐）

1. 在 https://supabase.com 新建一个项目
2. 打开 SQL Editor，整体执行仓库中的 `supabase/schema.sql`
   - 该脚本会创建私有存储桶 `read-something-sync`
   - 并为每个用户划定独立目录，配合行级安全策略保证只能访问自己的数据
3. 在 Authentication → Users 中为自己新建一个用户（邮箱 + 密码）
4. 打开应用 → 设置 → 跨设备同步
   - 同步方式选择 **Supabase**
   - 项目地址填写 `https://xxxx.supabase.co`
   - anon key 从 Settings → API 中复制
   - 填写登录邮箱和密码，设置一个同步密码
   - 点击「测试连接」，通过后点击「立即上传」

### 方式二：GitHub Gist

1. 访问 https://github.com/settings/tokens/new
2. 勾选 `gist` 权限，生成并复制 Token
3. 打开应用 → 设置 → 跨设备同步，同步方式选择 **GitHub Gist**
4. 粘贴 Token，设置同步密码，点击「测试连接」
   - 若浏览器直连 GitHub 被拦截，可额外填写一个 CORS 中转地址

### 同步方式与范围

- **智能同步**：以上次成功同步的版本为基准；仅一端变化时自动上传或下载，两端都变化时停止并提示选择
- **立即上传 / 从云端恢复**：手动指定方向
- **自动同步**：数据变化后延迟 3 秒上传
- 同步内容可在设置中逐项开关：书籍正文、图片、TTS 音频
  - 建议保持「TTS 朗读音频」关闭，它体积大且可以重新生成

### 需要留意的地方

- 同步密码一旦忘记，云端存档无法恢复，请自行妥善备份
- 新设备首次恢复必须输入同一个同步密码
- 为实现自动同步，同步密码会保存在当前浏览器本地；请只在可信设备上启用同步
- 自动上传发现另一台设备已经产生新版本时会停止，不会静默覆盖云端数据
- 旧版本存放在 `localStorage` 的 Gist 配置会在首次打开时自动迁移

## 与个人博客集成

如果你想在你的博客 `https://aning-q.github.io/` 中添加阅读应用入口，可以：

**选项 A：保持独立部署（推荐）**
- 访问地址：`https://aning-q.github.io/read-something/`
- 在你的博客中添加一个导航链接指向此地址

**选项 B：部署到博客子目录**
1. 修改 `vite.config.ts` 中的 `base` 为 `/reader/`
2. 构建后将 `dist` 内容复制到博客仓库的 `/reader` 目录下

## 注意事项

- 同步设置、Token 与刷新令牌仅存储在浏览器本地
- anon key 可以公开，但请勿把 service_role key 填入前端
- 云端存档是加密后的密文，平台无法直接读取内容
- 建议定期导出备份完整数据
